# =============================================================================
# Atelier - Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# Application
# -----------------------------------------------------------------------------
APP_NAME="Your Artist Catalogue"
DEBUG=false
LOG_LEVEL=INFO

# Database
# -----------------------------------------------------------------------------
# SQLite database path (default location)
DATABASE_URL=sqlite+aiosqlite:///data/artworks.db

# API Server
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# =============================================================================
# Scraping Settings
# =============================================================================
# How often to run automated discovery (in minutes)
SCRAPE_INTERVAL_MINUTES=60

# Delay between requests to avoid rate limiting (in seconds)
REQUEST_DELAY_SECONDS=2.0

# Maximum concurrent scraping requests
MAX_CONCURRENT_REQUESTS=3

# =============================================================================
# OPTIONAL: eBay API Integration
# =============================================================================
# Get credentials at: https://developer.ebay.com/
# Free tier includes 5,000 API calls per day
#
# 1. Create account at developer.ebay.com
# 2. Create an application (choose production keys)
# 3. Copy App ID (Client ID) and Cert ID (Client Secret)
#
# EBAY_CLIENT_ID=your-ebay-app-id
# EBAY_CLIENT_SECRET=your-ebay-cert-id
# EBAY_DEV_ID=your-dev-id
# EBAY_ENVIRONMENT=production

# =============================================================================
# OPTIONAL: Email Notifications
# =============================================================================
# Configure to receive alerts when new potential artworks are discovered
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# NOTIFICATION_EMAIL=alerts@example.com
